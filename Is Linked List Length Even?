bool isLengthEven(struct Node **head)
{
    int cnt = 0;
    Node* temp = *head;

    while(temp != NULL)
    {
        cnt++;
        temp = temp -> next;
    }

    if(cnt % 2 == 0)
    {
        return true;
    }
    return false;
}
