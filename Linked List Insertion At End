Node *insertAtEnd(Node *head, int x)
{
    if(head == NULL)
    {
        Node* newNode = new Node(x);
        return newNode;
    }
    Node* temp = head;
    Node* first = head;
    while(temp -> next != NULL)
    {
        temp = temp -> next;
    }
    Node* newNode = new Node(x);
    temp -> next = newNode;
    return first;
}
